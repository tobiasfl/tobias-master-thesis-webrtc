#!/usr/bin/env bpftrace

#include <linux/socket.h>
#include <linux/tcp.h>
#include <net/sock.h>

BEGIN
{
        printf("test");
}

tracepoint:tcp:tcp_probe
{

    //printf("%s %u\n", comm, args->snd_cwnd);
}

END
{
    
}
